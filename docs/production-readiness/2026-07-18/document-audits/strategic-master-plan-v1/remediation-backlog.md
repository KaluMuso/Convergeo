# Remediation Backlog — Strategic Master Plan v1.0 audit

**Audit date:** 2026-07-18 · READ-ONLY audit — **no items implemented this session.** Each item is a controlled follow-up.
Baseline: `../../foundation/*` + repo `0b88723`. Priorities follow the contract: *any payment/security/identity/RLS/admin-role discrepancy is P0 until investigated* — where investigation showed intentional/by-design behaviour, the residual priority is noted.

| ID | Priority | Affected panel/service | Evidence | Proposed fix | Acceptance criteria | Dependencies | Suggested owner |
| -- | -------- | ---------------------- | -------- | ------------ | ------------------- | ------------ | --------------- |
| BL-01 | **P0** | Payments / ledger (`services/api` payments; `ledger_*`) | Q22/T5/D14 require escrow ledger legs on prepaid success; foundation R2: prepaid webhook success path updates `payments`+`audit_log` only — no `post_transaction(CHARGE_RECEIVED/ESCROW_HOLD)`; live 0 ledger txns | Trace prepaid MoMo/card success → ensure `CHARGE_RECEIVED`+`ESCROW_HOLD` post idempotently; add the missing hook if confirmed absent | A sandbox paid order produces balanced `ledger_transactions`/`ledger_postings` (escrow held); failure-path test proves no double-post; VERIFIED fixture committed | Sandbox Lenco creds; a non-audit change session | Payments / M08 |
| BL-02 | **P0** | Database / migrations (`supabase_migrations`) | foundation §4: live applied ≤0050 + odd `0052` (20260717100303); repo `0051/0053/0054/0055` NOT applied; live DB ≠ git tip | Reconcile migration ledger; decide apply order for 0051 (role hook), 0053 (translations), 0054 (service reviews), 0055 (services.bookable); fix 0052 version-key skew | `schema_migrations` matches an agreed target; each migration reversible/documented; release ledger records applied SHA↔migration | DBA review; maintenance window; F-items | Ops / DB |
| BL-03 | **P0→P1** | Identity / RBAC (`user_roles`; Auth hook; admin UI) | foundation §3.5/R6: `0051 custom_access_token` hook absent → manual role provisioning; `user_roles` RLS-on/0-client-policies; no admin users/roles CRUD UI | Apply 0051 + enable Auth custom-access-token hook when ready; add admin role-management UI; keep service-role-only writes | JWT carries roles from `user_roles`; admin can grant/revoke via UI with audit_log entry; least-privilege preserved | BL-02 (0051 apply) | Platform / Admin |
| BL-04 | **P1** | Automation / n8n (`release-job`, `tickets-issue`, `order-jobs`, `event-release`) | Q21/Q50/Q66; foundation R3: only 2 of 18 workflows live; escrow auto-release + ticket issuance absent | Import + activate registry workflows with `X-Internal-Token`; prove one tick each (dry-run) | Escrow auto-release fires at 48h; a paid ticket issues with dynamic QR; each workflow shows a successful execution | Internal tokens; n8n access | Ops / M13–M14 |
| BL-05 | **P1** | Observability (Sentry; UptimeRobot; analytics) | W8/Q65; foundation R4: no Vergeo5 Sentry projects; `analytics_events`/`funnel_events`=0; UptimeRobot NOT_AUDITABLE | Create Sentry projects (customer/vendor/admin/API) + set DSNs; configure uptime monitors; fire a test event; verify analytics ingestion after traffic | Sentry receives a test error from each app; uptime monitors green; admin dashboard tiles reflect non-zero aggregates post-traffic | DSN env config; founder | Founder / M16-P06 |
| BL-06 | **P1** | Seller acquisition (`apps/customer` vendor-app CTA) | Q3/Q35; foundation R1: `/en/sell` CTAs disabled ("temporarily unavailable"); `NEXT_PUBLIC_VENDOR_APP_URL` likely unset | Set `NEXT_PUBLIC_VENDOR_APP_URL=https://vendor.vergeo5.com` on `convergeo-customer`; redeploy; re-verify CTA href | Sell CTA links to live vendor onboarding; no `localhost` leak; click reaches vendor signup | Vercel env access | Founder / Vercel |
| BL-07 | **P1** | Catalogue / merchandising (public catalog, `listing_images`) | Q57/Risk#1; foundation R5: 3 demo vendors, 134 listings, 134 `demo/` images, 0 real orders | Onboard real vendors or clearly label demo; keep `public_launch=false` until real supply; exclude demo from public search/SEO at real launch | Public catalog shows real (or unambiguously-labelled demo) inventory; demo excluded from SEO index at launch | BL-06 (CTA); vendor pipeline (D10) | Founder / merchandising |
| BL-08 | **P1** | Backups / RPO (OCI Object Storage; n8n backup) | W8/D21 PITR-lite; foundation R3: no n8n backup workflow; OCI dump host NOT_AUDITABLE | Stand up/verify DB backup job (n8n or host cron → OCI Object Storage); document RPO/restore drill | A dated backup artifact exists; a restore drill succeeds; RPO documented | OCI access | Ops / M10 |
| BL-09 | **P2** | CI/CD hardening (`.github/workflows`) | Rule6; foundation R7: `secret-scan`/`i18n-lint`/Lighthouse `continue-on-error`; docs↔YAML deps-audit conflict; branch-protection NOT_AUDITABLE | Remove `continue-on-error` from `secret-scan`; make agreed gates blocking; align `docs/ops/ci.md`; confirm branch protection ("no bypass") | Merges blocked on secret-scan/type/test/contract; docs match YAML; branch-protection screenshot on file | Founder (GitHub settings) | Founder / platform |
| BL-10 | **P2** | Security hygiene (Supabase Auth; RLS) | Q61; foundation §3.4/§3.3: leaked-password protection disabled; FORCE RLS false on `product_relations`/`ticket_type_instances`/`ticket_type_price_tiers`; definer-fn EXECUTE advisor WARNs | Enable leaked-password protection; confirm FORCE-RLS decisions or enable; verify 0050 revoke coverage vs advisor | Advisor WARNs cleared or explicitly risk-accepted with rationale; FORCE RLS decisions documented | Supabase access | Platform / Security |
| BL-11 | **P2** | Config values verification (`commission_rates`, delivery thresholds, tier caps) | Q2/Q6/Q4; values NOT_AUDITABLE this session (DB egress 403) | When DB access restored, run safe SELECTs to confirm 9 category rates match D4; free-delivery ≥K200; free-tier cap 30 | Rate/threshold/cap values match the locked decisions; discrepancies raised as CONFLICT rows | Supabase MCP access | Ops / Finance |
| BL-12 | **P2** | Payments resilience (second gateway) | Q19: doc wanted DPO+Lenco redundancy; prod Lenco-only | Track "second gateway via existing abstraction seam" as resilience item (Flutterwave/PawaPay per D11) — decision, not urgent build | A documented decision on gateway redundancy + failover runbook exists | Business decision | Payments |
| BL-13 | **P2** | Plan-document governance | Whole document: tech-stack/timeline/phase/language conflicts are intentional supersessions (D18–D24, D7, D27) but the Master Plan is uncorrected | Add a "SUPERSEDED — see 00-decisions.md" banner + per-section deltas to the Master Plan; fix doc-internal conflicts (Platinum vs Gold; 20 vs 30 listings; $62 vs $50) | Master Plan visibly reconciled to locked decisions; no stale stack claims used as build reference | none | Founder / docs |
| BL-14 | **P2** | Auth providers / notifications confirmation | Q29/Q68: Facebook provider + Accounting integration NOT_AUDITABLE; OTP transport differs | Confirm enabled Supabase Auth providers (Google/Facebook) and whether an accounting integration is in scope | Provider set + accounting-scope decision documented | Supabase Auth access | Platform |

## Priority rollup

| Priority | Count | Items |
| -------- | ----- | ----- |
| **P0** | 3 | BL-01 (prepaid→ledger), BL-02 (migration drift), BL-03 (RBAC hook/UI — P0-raised, P1 residual) |
| **P1** | 5 | BL-04 (n8n workflows), BL-05 (observability), BL-06 (seller CTA), BL-07 (demo catalogue), BL-08 (backups) |
| **P2** | 6 | BL-09 (CI), BL-10 (security hygiene), BL-11 (config values), BL-12 (gateway resilience), BL-13 (doc governance), BL-14 (auth/notif confirm) |

> None of these are net-new to the platform's own risk register — they map onto foundation risks R1–R8. This document audit's contribution is to **tie each to a specific Master-Plan requirement** and to separate *intentional supersession* from *genuine gap*.
