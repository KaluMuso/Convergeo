# Lane D D1 external prerequisites — names only

No provider-sandbox request, live transaction, hosted callback publication, shared-staging change, or production-money enablement occurred. No Lenco or Supabase credentials were present in the task environment by name. The local drill uses synthetic signing material and a disposable PostgreSQL/PostgREST stack; it is **not** provider certification or real Auth issuance.

| Prerequisite/capability | Configuration name or route | Responsible owner to confirm | Verified available here? |
| --- | --- | --- | --- |
| Isolated Lenco sandbox account and documented REST host | `LENCO_ENV=sandbox`, `LENCO_SANDBOX_BASE_URL` | Payments operator + Lenco | **No**. The default sandbox host in code is an assumption, not a verified working endpoint. |
| Sandbox API authorization and correct merchant account | `LENCO_API_TOKEN`, `LENCO_ACCOUNT_ID` | Payments operator | **No**; names exist in source/example configuration only. |
| Nonproduction collection rail/test payer and limits | Lenco mobile-money collection capability; `LENCO_ENABLE_ZAMTEL_COLLECTIONS` only if Zamtel is confirmed | Payments operator + Lenco | **No**. No test phone/account, authorized amounts, or rail capabilities supplied. |
| Provider-signed callback delivery into an isolated target | `POST /webhooks/lenco`, `X-Lenco-Signature`, registered sandbox callback URL | Payments operator + Lenco + API owner | **No**. Local endpoint and HMAC verifier exist; no public callback was published or registered. Confirm callback retries, event IDs, ordering, payload amount/currency/reference semantics and secret rotation. |
| Isolated API/DB/auth project | `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_DB_URL` | Infrastructure owner | **No hosted isolated target**. The D1 harness uses local PostgreSQL 16 and PostgREST 14 with synthetic JWT claims. |
| Internal worker authorization | `INTERNAL_RECONCILIATION_TOKEN`, `INTERNAL_PAYOUTS_TOKEN` | API/operator owner | Names and local endpoints exist; no hosted worker invocation authorized. |
| Safe payment and payout gates for sandbox only | `PAYMENTS_ENABLED`, `PAYMENTS_ALLOW_PRODUCTION`, `PAYOUTS_ENABLED` | Release/finance owner | Defaults remain **off**. Do not enable until sandbox account, target, limits and authorization are verified. |
| Provider collection status, account balance and transactions APIs | Lenco collections/status, accounts, transactions | Lenco + payments operator | Contract adapters exist; sandbox behavior and authoritative provider reference not verified. |
| Provider-native partial/full refund capability | No repository native Lenco refund API; local model uses customer payout transfer | Lenco + finance owner | **Unknown/blocked**. Confirm whether native refund exists and whether partial refunds are supported; do not infer support from local refund breakdowns. |
| Provider payout rails, payee resolution, status and retry semantics | Lenco transfers/mobile-money, transfers/bank-account, transfers/status, resolve APIs; `LENCO_ACCOUNT_ID` | Lenco + finance owner | Code and local simulations exist; sandbox support, idempotency on response loss and payee settlement not verified. |
| Reconciliation statement and operational mismatch recovery | Lenco accounts/transactions; `POST /internal/reconciliation/daily-report` | Finance operations | No sandbox statement, signed settlement reference or operator closeout proof. |

For a later authorized provider-sandbox packet, bind a single candidate SHA/image to an isolated target, verify the sandbox host/account/token/callback registration and allowed test amounts **without displaying values**, then execute successful/declined/timeout/duplicate/reordered collection, mismatch, reconciliation, refund capability and payout/retry drills. Capture provider-assigned references, sanitized API/database/ledger evidence and exact reversal/cleanup decisions. The D1 local test command and database setup are in `local-harness.md`; none of its green results can substitute for that external tier.
