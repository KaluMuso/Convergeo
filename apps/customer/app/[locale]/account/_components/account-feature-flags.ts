/**
 * E19 — extend shop bottom nav into account routes for thumb-reach (Orders tab).
 * Disabled until product sign-off; flip to `true` after UX review.
 */
export const ACCOUNT_BOTTOM_NAV_ENABLED = false;
